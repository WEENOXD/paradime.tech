<div class="terminal-card">
	<div class="terminal-header">
		<span class="pixel terminal-label">SMS SUMMARY • 11:47 PM</span>
	</div>
	<div class="terminal-body">
		<p><strong>New Lead:</strong> Sarah Martinez</p>
		<p><strong>Issue:</strong> Water heater leak, needs emergency service</p>
		<p><strong>Location:</strong> 2.3 miles from your shop</p>
		<p><strong>Urgency:</strong> High – wants quote ASAP</p>
		<p><strong>Phone:</strong> (555) 234-5678</p>
	</div>
	<div class="terminal-footer pixel">
		CAPTURED • FORWARDED • READY TO FOLLOW UP
	</div>
</div>

<style>
	.terminal-card {
		background: var(--color-surface);
		border: 1px solid var(--color-border-strong);
		border-radius: var(--radius-lg);
		overflow: hidden;
		box-shadow: var(--shadow-lg);
	}

	.terminal-header {
		background: var(--color-tan);
		padding: var(--space-sm) var(--space-md);
		border-bottom: 1px solid var(--color-border);
	}

	.terminal-label {
		color: var(--color-ink-light);
		font-size: 0.6875rem;
	}

	.terminal-body {
		padding: var(--space-lg);
	}

	.terminal-body p {
		margin-bottom: var(--space-sm);
		font-size: 0.9375rem;
		line-height: 1.6;
	}

	.terminal-body p:last-child {
		margin-bottom: 0;
	}

	.terminal-body strong {
		font-weight: 500;
		color: var(--color-ink);
	}

	.terminal-footer {
		background: var(--color-accent);
		color: white;
		padding: var(--space-sm) var(--space-md);
		text-align: center;
		font-size: 0.6875rem;
	}
</style>
